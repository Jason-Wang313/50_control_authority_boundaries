# Plan

1. Preserve the v2 tuned-threshold result as a negative control, not as the final contribution.
2. Reframe the live claim around compositional physical authority boundary fields under user, robot, contact, sensing, and shift stress.
3. Run the deterministic full-scale benchmark across scenarios, robot platforms, user regimes, risk regimes, sensing regimes, shift regimes, and policies.
4. Generate compact CSV rows, summaries, validation JSON, LaTeX tables, and PDF figures.
5. Write a 25-page anonymous ICLR-style manuscript that imports generated tables and figures.
6. Build `C:/Users/wangz/Downloads/50.pdf`, remove local `paper/main.pdf`, record hash metadata, visually inspect rendered pages, and push the final repo state.
